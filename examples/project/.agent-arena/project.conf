# Minimal project adapter. Copy this directory into a Git project with
# `agent-arena init --repo /path/to/project` instead of editing this example.
project_name="example-project"
validation_script=".agent-arena/validate.sh"
